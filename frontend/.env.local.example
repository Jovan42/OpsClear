VITE_API_URL=http://localhost:8080
VITE_KEYCLOAK_URL=http://localhost:8180
VITE_KEYCLOAK_REALM=opsclear
VITE_KEYCLOAK_CLIENT_ID=opsclear-frontend

# Paddle (ADR-0044, JOB-171) — public client-side token for Paddle.js, sandbox
# during JOB-171-179. Not secret the way the backend API key is, but still
# left out of source control since it's environment-specific.
VITE_PADDLE_CLIENT_TOKEN=
