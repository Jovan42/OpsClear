// Placeholder — real specs for this feature area land in JOB-210 (ADR-0049 Appendix,
// "API Keys"). Kept as a skipped stub only so this folder exists in git and the
// directory structure matches the ADR-0049 breakdown up front.
describe('API Keys', () => {
  it.skip('placeholder — see JOB-210', () => {});
});
