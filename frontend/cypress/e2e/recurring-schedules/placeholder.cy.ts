// Placeholder — real specs for this feature area land in JOB-222 (ADR-0049 Appendix,
// "Recurring Schedules (+ Missed Runs + Cron Preview)"). Kept as a skipped stub only so this folder exists in git and the
// directory structure matches the ADR-0049 breakdown up front.
describe('Recurring Schedules (+ Missed Runs + Cron Preview)', () => {
  it.skip('placeholder — see JOB-222', () => {});
});
