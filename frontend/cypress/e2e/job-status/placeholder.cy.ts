// Placeholder — real specs for this feature area land in JOB-214 (ADR-0049 Appendix,
// "Job Status Transitions & Blocking"). Kept as a skipped stub only so this folder exists in git and the
// directory structure matches the ADR-0049 breakdown up front.
describe('Job Status Transitions & Blocking', () => {
  it.skip('placeholder — see JOB-214', () => {});
});
