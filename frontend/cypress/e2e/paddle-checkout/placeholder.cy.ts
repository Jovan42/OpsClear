// Placeholder — real specs for this feature area land in JOB-230 (ADR-0049 Appendix,
// "Paddle Checkout / Upgrade / Downgrade / Cancellation"). Kept as a skipped stub only so this folder exists in git and the
// directory structure matches the ADR-0049 breakdown up front.
describe('Paddle Checkout / Upgrade / Downgrade / Cancellation', () => {
  it.skip('placeholder — see JOB-230', () => {});
});
