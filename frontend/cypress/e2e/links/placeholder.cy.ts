// Placeholder — real specs for this feature area land in JOB-221 (ADR-0049 Appendix,
// "Job & Project Links"). Kept as a skipped stub only so this folder exists in git and the
// directory structure matches the ADR-0049 breakdown up front.
describe('Job & Project Links', () => {
  it.skip('placeholder — see JOB-221', () => {});
});
