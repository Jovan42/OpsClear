// Placeholder — real specs for this feature area land in JOB-223 (ADR-0049 Appendix,
// "Job Types"). Kept as a skipped stub only so this folder exists in git and the
// directory structure matches the ADR-0049 breakdown up front.
describe('Job Types', () => {
  it.skip('placeholder — see JOB-223', () => {});
});
