// Placeholder — real specs for this feature area land in JOB-231 (ADR-0049 Appendix,
// "Billing History & Past-Due State"). Kept as a skipped stub only so this folder exists in git and the
// directory structure matches the ADR-0049 breakdown up front.
describe('Billing History & Past-Due State', () => {
  it.skip('placeholder — see JOB-231', () => {});
});
