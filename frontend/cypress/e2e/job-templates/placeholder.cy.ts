// Placeholder — real specs for this feature area land in JOB-220 (ADR-0049 Appendix,
// "Job Templates — Project & Org-Level"). Kept as a skipped stub only so this folder exists in git and the
// directory structure matches the ADR-0049 breakdown up front.
describe('Job Templates — Project & Org-Level', () => {
  it.skip('placeholder — see JOB-220', () => {});
});
