// Placeholder — real specs for this feature area land in JOB-218 (ADR-0049 Appendix,
// "Projects (CRUD, Lifecycle, Members)"). Kept as a skipped stub only so this folder exists in git and the
// directory structure matches the ADR-0049 breakdown up front.
describe('Projects (CRUD, Lifecycle, Members)', () => {
  it.skip('placeholder — see JOB-218', () => {});
});
