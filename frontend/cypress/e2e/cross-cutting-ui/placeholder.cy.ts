// Placeholder — real specs for this feature area land in JOB-228 (ADR-0049 Appendix,
// "Mobile Nav, Dark Mode, Empty States (cross-cutting UI)"). Kept as a skipped stub only so this folder exists in git and the
// directory structure matches the ADR-0049 breakdown up front.
describe('Mobile Nav, Dark Mode, Empty States (cross-cutting UI)', () => {
  it.skip('placeholder — see JOB-228', () => {});
});
