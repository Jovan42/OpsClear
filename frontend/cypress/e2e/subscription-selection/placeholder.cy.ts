// Placeholder — real specs for this feature area land in JOB-229 (ADR-0049 Appendix,
// "Subscription Selection & Feature Gating"). Kept as a skipped stub only so this folder exists in git and the
// directory structure matches the ADR-0049 breakdown up front.
describe('Subscription Selection & Feature Gating', () => {
  it.skip('placeholder — see JOB-229', () => {});
});
