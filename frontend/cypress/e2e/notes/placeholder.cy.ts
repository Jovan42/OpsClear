// Placeholder — real specs for this feature area land in JOB-215 (ADR-0049 Appendix,
// "Notes"). Kept as a skipped stub only so this folder exists in git and the
// directory structure matches the ADR-0049 breakdown up front.
describe('Notes', () => {
  it.skip('placeholder — see JOB-215', () => {});
});
