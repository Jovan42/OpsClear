// Placeholder — real specs for this feature area land in JOB-217 (ADR-0049 Appendix,
// "Status History"). Kept as a skipped stub only so this folder exists in git and the
// directory structure matches the ADR-0049 breakdown up front.
describe('Status History', () => {
  it.skip('placeholder — see JOB-217', () => {});
});
