// Placeholder — real specs for this feature area land in JOB-208 (ADR-0049 Appendix,
// "Auth Flows (Login, Registration, Password Reset)"). Kept as a skipped stub only so this folder exists in git and the
// directory structure matches the ADR-0049 breakdown up front.
describe('Auth Flows (Login, Registration, Password Reset)', () => {
  it.skip('placeholder — see JOB-208', () => {});
});
