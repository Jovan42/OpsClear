// Placeholder — real specs for this feature area land in JOB-212 (ADR-0049 Appendix,
// "Job List & Filtering"). Kept as a skipped stub only so this folder exists in git and the
// directory structure matches the ADR-0049 breakdown up front.
describe('Job List & Filtering', () => {
  it.skip('placeholder — see JOB-212', () => {});
});
