// Placeholder — real specs for this feature area land in JOB-227 (ADR-0049 Appendix,
// "Localization (i18n) — Smoke Coverage"). Kept as a skipped stub only so this folder exists in git and the
// directory structure matches the ADR-0049 breakdown up front.
describe('Localization (i18n) — Smoke Coverage', () => {
  it.skip('placeholder — see JOB-227', () => {});
});
