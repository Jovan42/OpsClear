// Placeholder — real specs for this feature area land in JOB-211 (ADR-0049 Appendix,
// "User Settings & Preferences"). Kept as a skipped stub only so this folder exists in git and the
// directory structure matches the ADR-0049 breakdown up front.
describe('User Settings & Preferences', () => {
  it.skip('placeholder — see JOB-211', () => {});
});
