// Placeholder — real specs for this feature area land in JOB-226 (ADR-0049 Appendix,
// "Public Landing Page & /features Interactive Demos"). Kept as a skipped stub only so this folder exists in git and the
// directory structure matches the ADR-0049 breakdown up front.
describe('Public Landing Page & /features Interactive Demos', () => {
  it.skip('placeholder — see JOB-226', () => {});
});
