// Placeholder — real specs for this feature area land in JOB-213 (ADR-0049 Appendix,
// "Job Create/Edit (incl. templates, markdown toolbar)"). Kept as a skipped stub only so this folder exists in git and the
// directory structure matches the ADR-0049 breakdown up front.
describe('Job Create/Edit (incl. templates, markdown toolbar)', () => {
  it.skip('placeholder — see JOB-213', () => {});
});
