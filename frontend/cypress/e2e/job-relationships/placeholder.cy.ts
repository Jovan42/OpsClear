// Placeholder — real specs for this feature area land in JOB-216 (ADR-0049 Appendix,
// "Job Relationships"). Kept as a skipped stub only so this folder exists in git and the
// directory structure matches the ADR-0049 breakdown up front.
describe('Job Relationships', () => {
  it.skip('placeholder — see JOB-216', () => {});
});
