parent=keycloak
import=common/keycloak
locales=en
# `styles` replaces the parent's list rather than appending to it — include the base
# theme's own login.css first (it neutralizes PatternFly's raw dark background pattern),
# then our own theme.css on top so our overrides still win.
styles=css/login.css css/theme.css
